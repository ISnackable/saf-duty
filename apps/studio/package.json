{
  "name": "studio",
  "version": "0.0.0",
  "scripts": {
    "dev": "drizzle-kit studio --config ./config.ts --verbose",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@repo/database": "workspace:*",
    "drizzle-orm": "^0.40.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.5"
  }
}
