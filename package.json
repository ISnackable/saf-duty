{
  "name": "duty-roster",
  "version": "1.0.0",
  "description": "A duty roster",
  "author": "Tommy",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm run --parallel -r build"
  },
  "devDependencies": {
    "@sanity/cli": "^3.32.0"
  }
}
